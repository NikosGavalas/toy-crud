# toy-crud
Minimal CRUD app (a todo list), to play around with React and node

Stack: React, MongoDB, Node, Express

To start the backend dev server (nodemon), go to backend dir and run 'npm start'
For the frontend, go respectively to the frontend dir and run 'npm start'

